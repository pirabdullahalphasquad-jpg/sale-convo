import { PlaceholderStep } from "./PlaceholderStep";

export function Step5Configure() {
  return (
    <PlaceholderStep
      step={5}
      eyebrow="Behaviour & Tone"
      title="Configure how your AI employee talks"
      description="Set tone of voice, languages, human handoff rules and the never-guess safety policy."
    />
  );
}
